cache
__pycache__
.venv
# Local secrets (MAPILLARY_ACCESS_TOKEN, PS_INTERNAL_API_KEY) — never commit.
.env
*.jsonl
*.submitted
nohup.out
smoke_check

# Run directories: track only the small, high-value files (provenance + hand-labeled
# verdicts); results, resume caches, and spot-check galleries stay local.
runs/**
!runs/*/
!runs/*/manifest.json
!runs/*/area.geojson
!runs/*/*_verdicts.json
