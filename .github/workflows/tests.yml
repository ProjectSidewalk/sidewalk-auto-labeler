name: tests

on:
  push:
    branches: [master]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      # The gallery-JS contract test skips without node; fail loudly here instead
      # of letting it skip silently in CI.
      - run: node --version
      # Tests need only the light deps (no torch).
      - run: pip install -r requirements-test.txt
      - run: pytest
